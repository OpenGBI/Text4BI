import React from 'react'
import { Chart } from '@antv/g2'
import { cateAndValue } from '../types'

interface IconProps {
  entityUser: string
}

const Icon: React.FC<IconProps> = ({ entityUser }) => <div>|||这里是一个icon|||</div>

export default Icon
